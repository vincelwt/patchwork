{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the Patchwork window is allowed to ask the system for: pick a folder, and open a link in the real browser. Without this every plugin call is denied and the button that calls it does nothing at all.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:allow-open-url"
  ]
}
